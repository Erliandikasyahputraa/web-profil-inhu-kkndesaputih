# SEO & Production Readiness Checklist

## SEO Complete
- [x] Basic `<title>` and `<meta name="description">` on all pages.
- [x] Language attribute `<html lang="id">` declared.
- [x] OpenGraph meta tags (`og:title`, `og:description`, `og:image`, `og:url`, `og:type`).
- [x] Twitter Card meta tags (`twitter:card`, `twitter:image`).
- [x] Canonical URL linking.
- [x] `robots.txt` configuration (allowing all, mapping to sitemap).
- [x] Static `sitemap.xml` generated.
- [x] Web Manifest (`site.webmanifest`) created for progressive web app / mobile bookmarking metadata.

## PWA / Icons
- [x] Favicon configured.
- [x] Apple touch icons (deferred to client asset delivery).

## Next Steps
This checklist is technically complete. Once the client provides the final canonical domain and `og-image.jpg`, SEO is ready for production.
